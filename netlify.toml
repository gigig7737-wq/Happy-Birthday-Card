[build]
  base = "./"
  publish = "./dist/"
  command = "npm run init-index-local && npm run build:parcel"

[template.environment]
  NAME = "enter Name of the receiver"
  PIC = "enter web url of the receiver's image"
